<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>locrec_estimator</name>
  <version>0.1.0</version>
  <description>
    The ROS 2 node: it runs the localizability-aware odometry over incoming scans, publishes the
    degeneracy signal and the estimate, recommends where to mount a marker or where to look, and
    shares the strips it mounts with the rest of the team.
  </description>
  <maintainer email="64199670+SaurabhKhimesra@users.noreply.github.com">Saurabh Khimesra</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>rclcpp</depend>
  <depend>nav_msgs</depend>
  <depend>sensor_msgs</depend>
  <depend>std_msgs</depend>
  <depend>builtin_interfaces</depend>
  <depend>locrec_msgs</depend>
  <depend>locrec_core</depend>
  <depend>eigen</depend>

  <test_depend>ament_cmake_gtest</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
